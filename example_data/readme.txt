These file contain some example projects stored in a BinaryCrate database and the file system.

To use them install BC as normal using fab development.setup

Then copy the db.sqlite3 file and private folders over there equivalents in the
~/binarycrate-mvp2/binarycrate folder
